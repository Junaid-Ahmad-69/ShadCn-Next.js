export default function RoutingLayout({children}: {
    children: React.ReactNode;
}) {
    return (
        <>
            <h2>Login and Register Screen Layout </h2>

            {children}
        </>
    )
}